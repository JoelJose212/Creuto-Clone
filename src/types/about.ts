export interface LeadershipMember {
  name: string;
  role: string;
  linkedin: string;
  initials: string;
}

export interface ProblemItem {
  text: string;
}

export interface NeverDoneItem {
  text: string;
}

export interface WhyChooseItem {
  icon: string;
  title: string;
  description: string;
}

export interface HowWeWorkStep {
  number: string;
  title: string;
  description: string;
}

export interface RightPartnerItem {
  text: string;
}
